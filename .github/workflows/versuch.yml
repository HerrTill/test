name: Check if anything has been merged into master  
  
on:  
  push:
    branch:
      - versuch
  
jobs:  
  check-merged:  
    runs-on: ubuntu-latest  
    steps:  
      - name: Checkout code  
        uses: actions/checkout@v3 
  
      - name: Create pull request
        if: github.ref == 'refs/heads/main' && (github.event.head_commit.timestamp | fromdateiso8601) > (now - duration('14d')).timestamp 
        run: echo "Yes"